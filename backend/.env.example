# Required
PLEX_BASE_URL=http://your-plex-host:32400
PLEX_TOKEN=xxxxxxxxxxxxxxxxxxxx
# Comma-separated Plex library section IDs that should use prerolls (optional; fallback: all)
PLEX_SECTION_IDS=

# Where preroll files live (container path)
PREROLL_DIR=/app/prerolls

# Security
ADMIN_API_KEY=change-this-to-something-long

# CORS for the UI
CORS_ORIGIN=http://localhost:5173,http://localhost:8080,http://localhost:3000
